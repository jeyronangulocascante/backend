/* eslint-disable no-unused-vars */
// eslint-disable-next-line no-shadow
export enum Level {
  warn = 'warn',
  error = 'error',
  info = 'info',
  verbose = 'verbose',
  debug = 'debug',
  silly = 'silly',
}
